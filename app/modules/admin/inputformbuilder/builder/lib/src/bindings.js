//panels
//TODO: since there is no helpSliderVisible and helpSliderFx defined anywhere in current FB,

//$('#siteWrapper').on('click', '#helpSlideHandle', function (e) {
//  'use strict';
  //Comment out the following for JSHINT and we will review later.
  //if (helpSlideVisible) {
  //  helpSliderFx.reverse();
  //} else {
  //  helpSliderFx.play();
  //}
  //helpSlideVisible = !helpSlideVisible;
  //e.preventDefault();
//});

//TODO: since there is no folderSlideVisible and folerSliderFx defined anywhere in current FB,

//$('#siteWrapper').on('click', '#folderSlideHandle', function (e) {
//  'use strict';
  //Comment out the following for JSHINT and we will review later.
  //if (folderSlideVisible) {
  //  folderSliderFx.reverse();
  //} else {
  //  folderSliderFx.play();
  //}
  //folderSlideVisible = !folderSlideVisible;
  //e.preventDefault();
//});

//keyboard
//TODO: need to cooment out as JSHINT complains Will come back later.
//function initClipboardKeys(copy, paste, cut, Mousetrap) {
//  'use strict';
//  Mousetrap.bind('ctrl+c', function (e) {
//    if (e.preventDefault) {
//      e.preventDefault();
//    } else {
//      e.returnValue = false;              // internet explorer
//    }
//    copy.call();
//  });
//  Mousetrap.bind('ctrl+v', function (e) {
//    if (e.preventDefault) {
//      e.preventDefault();
//    } else {
//      e.returnValue = false;              // internet explorer
//    }
//    paste.call();
//  });
//  Mousetrap.bind('ctrl+x', function (e) {
//    if (e.preventDefault) {
//      e.preventDefault();
//    } else {
//      e.returnValue = false;              // internet explorer
//    }
//    cut.call();
//  });
//  /*
//   $(document).bind('keydown', 'ctrl+c', copy);
//   $(document).bind('keydown', 'ctrl+v', paste);
//   $(document).bind('keydown', 'ctrl+x', cut);
//   */
//}

//window
